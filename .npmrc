# 供應鏈防護：不執行相依套件的 install/postinstall 腳本（2025–2026 的 npm 蠕蟲主要靠這條路徑）。
# 本專案的原生相依（sharp、Tailwind oxide、unrs-resolver）都走 optionalDependencies 預編譯二進位，不需要 install script。
ignore-scripts=true
# 只接受 lockfile 內的版本，避免 CI/Vercel 建置時吃到剛發布的惡意版本
save-exact=true
